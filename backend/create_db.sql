-- SQL script to create the database manually
-- Run this in PostgreSQL if the setup script doesn't work

-- Create database
CREATE DATABASE hospital_db;

-- Connect to the database (run this separately after connecting to hospital_db)
-- The tables will be created automatically by the application
-- Or you can run: python setup_db.py

